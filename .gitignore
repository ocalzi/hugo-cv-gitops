# Secrets — never commit
*.secret
*.env
*.key
*.pem
*.p12
*.pfx
*-credentials.yaml
*-credentials.json
*-secret.yaml
htpasswd
kubeconfig
kubeconfig.*

# Example files are committed, actual secrets are not
!*.example

# OS
.DS_Store
Thumbs.db
